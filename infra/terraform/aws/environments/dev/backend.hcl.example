# Replace every placeholder only after the remote-state resources exist through
# a separately reviewed bootstrap process. Never commit account-specific values.
bucket         = "REPLACE-terraform-state-bucket"
key            = "secure-gitops-platform-portfolio/dev/aws.tfstate"
region         = "us-east-1"
encrypt        = true
kms_key_id     = "REPLACE-state-kms-key-arn"
dynamodb_table = "REPLACE-terraform-locks-table"
